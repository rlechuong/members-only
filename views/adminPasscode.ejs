<%- include("partials/header.ejs") %>

<h1>Admin Passcode</h1>

<% if (error) { %>
  <p><%= error %></p>
<% } %>

<form action="/admin-passcode" method="POST" class="admin-passcode-form">
	<label for="passcode">Passcode</label>
	<input id="passcode" name="passcode" type="password" required />

	<button type="submit">Verify</button>
</form>

<p class="demo-note">For demo purposes, the passcode is: <%= process.env.ADMIN_PASSCODE %></p>

<a href="/" class="cancel-link">Cancel</a>

<%- include("partials/footer.ejs") %>